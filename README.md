# Würfelblock

Simple Next.js dice game.


## Development

Use pnpm as the package manager. Install deps and run the dev server:

```bash
pnpm install && pnpm dev
```

Built with Next.js (App Router), React and TypeScript; styling via PostCSS. Key folders: `app/`, `components/`, `hooks/`, `lib/`, `providers/`, `public/`.


